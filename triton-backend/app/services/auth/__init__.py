"""Auth service sub-package.

Sub-modules:
  ``bootstrap``      — initial system-setup (first-admin registration) flow.
  ``local_auth``     — email/password login, registration, and password helpers.
  ``oidc_preflight`` — two-phase OIDC settings verification via redirect flow.
  ``oidc_settings``  — admin read/write of persisted OIDC provider settings.
"""
